"""
ClingySOCKs Agent — Package init.

Modules:
  - loop.py      — ReAct agent loop (plan, think, act, observe, reflect)
  - heartbeat.py — Periodic wake-up system (cron / background task)
  - planner.py   — Task planning prompts
"""
